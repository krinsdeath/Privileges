name: Privileges
main: net.krinsoft.privileges.Privileges
author: krinsdeath
version: ${project.version}-${project.build.number}
load: startup

commands:
  privileges:
    aliases:
      # /priv *
      - priv
      # /priv reload
      - pr
      # /priv check
      - pc
      # /priv group*
      - pg
      - pgc
      - pgr
      - pgs
      - pgp
      - pgps
      - pgpr
    description: The commands for manipulating players and settings
    usage: |